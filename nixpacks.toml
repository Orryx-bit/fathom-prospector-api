[phases.setup]
nixPkgs = ['python311', 'gcc', 'libffi', 'openssl']

[phases.install]
cmds = [
    'pip install --upgrade pip',
    'pip install -r requirements.txt'
]

[start]
cmd = 'uvicorn api_server:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 300'
